%h2.title Rankings

%p
  %b Server:
  = @server
%p
  %b Ranking:
  %span Number of Titles

- if(@characters == 0)
  %span Couldn't find any characters for this ranking.
- else
  %table.listing#listing
    %thead
      %tr
        %th Name
        %th Server
        %th
          %a{:href => "/rankings/titles?sort=#{@sort_text}"} Titles
    %tbody
      - @characters.each do |character|
        %tr
          %td
            %a{:href => "/#{character["s"]}/#{character["n"]}"}= character["n"]
          %td= character["s"]
          %td= character["num_titles"]